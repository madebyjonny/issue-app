[build]
builder = "nixpacks"

[deploy]
startCommand = "php artisan migrate:fresh --force --seed && php artisan optimize && php artisan serve --host=0.0.0.0 --port=$PORT"
healthcheckPath = "/up"
restartPolicyType = "on_failure"
